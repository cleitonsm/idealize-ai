# idealize-contracts (Python)

Pydantic models aligned with the TypeScript package `@idealize-ai/contracts`. Install editable from the monorepo root before the backend:

```bash
pip install -e packages/contracts/python
pip install -e apps/backend
```

When fields change, update `packages/contracts/src` and this package together.
